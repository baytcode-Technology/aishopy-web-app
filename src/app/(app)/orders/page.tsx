import { ComingNext } from '@/components/catalog/ComingNext'

export default function OrdersPage() {
  return (
    <ComingNext
      title="Orders"
      body="Order list, fulfillment, and customer details come next. Catalog stays the working area for now."
    />
  )
}
